# テストコード用の gdlint 設定。
#
# gdlint は実行時のカレントディレクトリから上へ設定ファイルを探すため、
# scripts/static-check.sh は tests/ 配下を検査するときだけ tests/ へ移動して実行する。
# 製品コードには既定値（max-public-methods: 20）をそのまま適用する。
#
# テストは 1 ケース 1 関数で書くため、ファイルあたりの関数が 20 を超える。
# 「巨大 Manager を避ける」という規約（要件定義 §127）はテストには当てはまらない。
max-public-methods: 100
